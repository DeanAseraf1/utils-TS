import { ReactElement, Children } from "react";

// export const If = (props:{condition:boolean, children?:JSX.Element}) => {
//     const {condition, children} = props;
//     if(!condition) return null;
//     return children ?? null;

// }

// export const IfCallback = (props:{callbackCondition: (condition:boolean) => JSX.Element | null}) => {

// }